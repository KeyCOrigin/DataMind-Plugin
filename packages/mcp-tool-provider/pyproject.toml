[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "datamind-mcp-provider"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "datamind>=0.3.2", "datamind-contracts>=1.0"]

[tool.setuptools.packages.find]
where = ["src"]
